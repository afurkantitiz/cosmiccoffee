# Netlify cache headers — fast repeat loads for immutable assets.
# menu.json / html / css / js are left to Netlify defaults (ETag revalidation)
# so price/menu updates propagate immediately.

/assets/fonts/*
  Cache-Control: public, max-age=31536000, immutable

/assets/products/*
  Cache-Control: public, max-age=604800
